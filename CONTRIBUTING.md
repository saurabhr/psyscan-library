# Contributing a task or experiment card

1. Pick your distro and add your card as a single file:
   - `psychscanner` task card → `tasks/psychscanner/<name>.json` (or `.tcard.psyscan`)
   - `psychscanner` experiment card → `experiments/psychscanner/<name>.json` (or `.xcard.psyscan`)
   - `primal` task card → `tasks/primal/<name>.json` (plain JSON only — primal has
     no experiment cards)
2. Validate it locally, with the matching package installed:
   ```bash
   pip install -e ".[psychscanner]"  # or ".[primal]" — never both, same import name
   python scripts/validate_contribution.py tasks/psychscanner/<name>.json
   ```
   This checks, in order: required fields are present, the filename/content
   isn't a duplicate of an existing card, and — the hard requirement — **the
   card actually runs end-to-end** against that distro's built-in mock LLM.
   A card that raises during a real run will not be merged. If the installed
   package doesn't match the card's distro subfolder, validation reports
   `SKIP` rather than pass/fail — CI validates each distro in its own job.
   On a full `PASS` it also regenerates `INDEX_LEDGER.json` — commit that
   change along with your card. (`python scripts/index_ledger.py build`
   still exists for a manual rebuild, e.g. after deleting a card by hand.)
3. Open a PR. CI re-runs the same checks, per distro, on every changed card.

## Existing seed cards

`tasks/psychscanner/example_survey.json` was copied in from `psychscanner`'s
own `examples/tasks/` to bootstrap this index — it's not auto-synced. This
repo is now the source of truth for it going forward; if `psychscanner`'s
copy ever changes, treat that as the one that drifted, not this one.
Likewise `tasks/primal/rm_singleturn_demo.json` was seeded from
`psychscanner-primal`'s examples.

**2026-08-22** — added every unique task from `psychscanner`'s
`examples/demonstration_suite/` (01–08) that has a portable, self-contained
task card and passes this repo's own merge gate. Also copied in, not
auto-synced — same "this repo is the source of truth going forward" rule
as above.

- `tasks/psychscanner/bfi44.json`, `vviq16.json` — Demo 03 (personality
  survey), copied from `psychscanner/examples/tasks/`.
- `tasks/psychscanner/pal50.json` — Demo 02 (paired-associate learning),
  same source. Initially failed the mock-LLM gate
  (`with_structured_output` wasn't implemented on `ChatMockModel` — a real
  bug, not a card problem); fixed upstream in `psychscanner`, then this
  card passed unmodified.
- `tasks/psychscanner/cognitive_rl_bandit.json` — Demo 01 (3-armed bandit).
  Didn't exist as a file anywhere; Demo 01 built it inline in Python
  (`make_bandit_task()` in `run_reward_task.py`) and never saved it. Written
  out here verbatim from that function so it's fetchable via
  `task_library()`.
- `tasks/psychscanner/vlm_shapes_demo.tcard.psyscan` — Demo 04 (VLM shape
  naming), from `psychscanner/examples/tasks/` — the reusable single-pass
  task-card version of Demo 04's task (6 trials, one per stimulus image,
  base64-embedded per this file's own "don't upload large files" note
  above), as opposed to the demo script's inline 2-block factorial
  reconstruction of the same 6 images.
- `tasks/psychscanner/prospect_theory_demo.tcard.psyscan` — Demo 08
  (Prospect Theory planning), from `psychscanner/examples/tasks/`.
- `tasks/primal/introspection_weights_demo.json` — Demo 07 (introspective
  self-report), the decision-only subset with real per-trial ground truth
  (primal's inclusion bar). Generated by actually running Demo 07's own
  `build_primal_task_card.py` generator, not hand-written.

Not added: Demo 05 (persona-vector extraction) and Demo 06
(Othello-GPT/ROME/CCS interpretability probes) operate directly on model
activations with no `ExpCard`/trial structure — there's no task card to
extract.

## What makes a good card

- `taskname` and `items` (task cards) or `task_file` (experiment cards) —
  see `psychscanner`'s [task_library guide](https://github.com/saurabhr/psychscanner/blob/main/docs/guides/task_library.md)
  for the full schema.
- Every trial needs a `trcode` and a `stimulus` (or pre-built `hmsg`).
- Keep names filesystem-safe and unique — the ledger rejects both filename
  collisions and byte-identical content under a different name.
- **Don't upload large files.** Multimodal cards embed images/audio as
  base64 directly inside the JSON (see `image_block()` in `psychscanner`) —
  there's no separate asset store, so a bulky stimulus bloats this repo's
  git history forever, not just the one file. Keep embedded media small:
  synthetic/generated stimuli (simple shapes, icons, short clips) rather
  than photos or long recordings, and downscale/compress before embedding.
  `tasks/psychscanner/vlm_shapes_demo.tcard.psyscan` is the reference
  example — 6 real images, whole card under 15 KB. If your real stimulus
  set is inherently large, host it externally and reference it by URL in
  `stimulus`/`source` instead of embedding it.
